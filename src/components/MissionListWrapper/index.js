import React from 'react';
import styled from 'styled-components';

const MissionListWrapper = styled.div`
  padding: 1rem;
  display: flex;
  flex-flow: column nowrap;
`;

export default MissionListWrapper;
