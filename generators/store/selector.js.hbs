import { createSelector } from 'reselect';
import get from 'lodash/fp/get';

const select{{ properCase name }} = get('{{ camelCase name }}');

const makeSelectValue = () => createSelector(
  select{{ properCase name }},
  get('value'),
);

export {
  makeSelectValue,
};
