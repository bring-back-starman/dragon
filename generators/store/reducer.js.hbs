import set from 'lodash/fp/set';
import flow from 'lodash/fp/flow';
import { SET_VALUE } from 'store/{{ name }}/constants';

const initialState = {
  value: '',
};

const {{ name }} = (state = initialState, action) => {
  switch (action.type) {
    case SET_VALUE:
      return flow(
        set('value', action.value),
      )(state);
    default:
      return state;
  }
};

export default {{ name }};
